name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  install-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Dependencies
        run: npm install

      - name: Deploy (Optional)
        if: github.ref == 'refs/heads/main'
        run: |
          echo "Deploying..."
          # Example deploy step:
          # rsync -avz . user@yourserver.com:/var/www/html/
